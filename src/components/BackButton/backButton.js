import styles from './back.module.css'


export default function BackButton() {
  return (
    <button
      type="button"
      className={styles.button}
    >
      Voltar ao início
    </button>
  )
}
